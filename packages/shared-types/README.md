# @snackbar/shared-types

Shared type definitions consumed by both the client and server packages. During the migration, add interfaces, enums, and helper utilities here to keep contracts consistent across the stack.
